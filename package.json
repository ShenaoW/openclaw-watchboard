{
  "name": "openclaw-watchboard",
  "version": "1.0.0",
  "description": "OpenClaw 安全风险监控面板",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:shared && npm run build:backend && npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:shared": "cd shared && npm run build",
    "refresh:databases": "python3 scripts/run_analysis.py",
    "refresh:skills-db": "python3 scripts/setup_database.py && python3 scripts/analyze_clawhub_skills.py && python3 scripts/analyze_csv_skills.py && python3 scripts/fetch_skills_sh_data.py && python3 scripts/fetch_skills_sh_rankings.py && python3 scripts/fetch_gendigital_skills.py --classification safe && python3 scripts/import_external_skills_to_db.py && python3 scripts/generate_statistics.py",
    "refresh:risks-db": "python3 scripts/setup_database.py && python3 scripts/analyze_vulnerabilities.py && python3 scripts/import_vulnerabilities_to_db.py",
    "refresh:exposure-db": "python3 scripts/setup_database.py && python3 scripts/analyze_exposure_data.py",
    "scan:deployment-security": "python3 tools/openclaw-scan/scan.py",
    "package:openclaw-scan": "cd tools/openclaw-scan && zip -r ../../frontend/public/downloads/openclaw-scan.zip . -x '*.DS_Store' 'openclaw_security_report.md' '__pycache__/*' '*.pyc'",
    "sync:databases": "./scripts/sync_databases_to_tencent.sh",
    "test": "npm run test:backend && npm run test:frontend",
    "test:frontend": "cd frontend && npm run test",
    "test:backend": "cd backend && npm run test",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "start": "cd backend && npm start",
    "install:all": "npm install && npm run install:frontend && npm run install:backend && npm run install:shared",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "install:shared": "cd shared && npm install"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.50.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "csv-parse": "^6.1.0"
  }
}
